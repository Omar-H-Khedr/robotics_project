# Milestone 03: Meta-RL

Purpose: introduce reinforcement learning after the simulation, context, and safety interfaces are stable.

Initial target:

- Wrap selected simulation tasks as RL environments.
- Include context features in observations.
- Keep actions bounded and filtered through the safety layer.
- Compare learned adaptation with fixed and context-adapted baselines.

This milestone should remain simulation-only until the research pipeline is mature.

