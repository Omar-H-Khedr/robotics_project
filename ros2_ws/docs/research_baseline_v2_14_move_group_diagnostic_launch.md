# Research Baseline v2.14m MoveGroup Diagnostic Launch

v2.14m disables world geometry/octomap in diagnostic-only mode because `moveit_ros_perception` is not installed and no usable `OccupancyMapUpdater` plugin class is declared.
